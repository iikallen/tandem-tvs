[project]
name = "tandem-tvs-backend"
version = "0.1.0"
description = "Stage 1 backend for the Tandem portal module"
requires-python = ">=3.13,<3.14"
dependencies = [
  "django==5.2.17",
  "django-redis>=6,<7",
  "djangorestframework>=3.16,<3.17",
  "drf-spectacular>=0.28,<0.29",
  "psycopg[binary]>=3.2,<4",
  "redis>=6,<8",
  "uvicorn[standard]>=0.35,<1",
]

[dependency-groups]
dev = [
  "basedpyright>=1.31,<2",
  "django-stubs>=5.2,<6",
  "djangorestframework-stubs>=3.16,<4",
  "pytest>=8.4,<9",
  "pytest-cov>=6,<8",
  "pytest-django>=4.11,<5",
  "ruff>=0.12,<1",
  "ty>=0.0.1a20,<1",
]

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "config.settings.test"
python_files = ["test_*.py"]
testpaths = ["tests"]
addopts = "--strict-markers --strict-config --cov=apps --cov=config --cov-report=term-missing --cov-fail-under=80"

[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "DJ"]

[tool.basedpyright]
pythonVersion = "3.13"
typeCheckingMode = "standard"
include = ["apps", "config", "tests"]
exclude = ["**/migrations"]
